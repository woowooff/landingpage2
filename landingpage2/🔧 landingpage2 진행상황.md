# 제주다회 랜딩페이지 진행상황

## 1. 프로젝트 개요
- 프로젝트명: 제주다회 랜딩페이지
- 목적: 제주다회 소개, 신청 폼, 참가 신청 자동 처리 흐름 구성
- 현재 위치: D:\CLAUDE\jeju_vibe\landingpage2

## 2. 완료된 작업
- 정적 랜딩페이지 구조 구성 완료
- 프리미엄 스타일 디자인 적용 완료
- 신청 폼 UI 구성 완료
- 클라이언트에서 신청 데이터 전송 기능 연결 완료
- Node.js + Express 서버 구성 완료
- 신청 접수 API 구현 완료
- 메일 전송/Google Sheets 저장/로컬 백업 로직 연결 완료
- 로컬 테스트로 신청 접수 흐름 확인 완료

## 3. 현재 상태 (2026-08-04 실제 확인함)
- 로컬 서버는 정상 실행 가능 상태입니다. (지금 3000 포트에 하나 떠 있음)
- 신청 폼 제출은 서버로 전달되고, 성공 응답을 받을 수 있습니다.
- **메일 발송·시트 저장은 둘 다 아직 동작하지 않습니다.** `.env`의 인증값 4칸이 **전부 예시 문구 그대로**입니다.
  - `GMAIL_APP_PASSWORD` = `PUT_YOUR_..._HERE` ← 이전 노트에 "등록되어 있다"고 쓰여 있었으나 **사실이 아니었음**
  - `GOOGLE_SERVICE_ACCOUNT_EMAIL` / `GOOGLE_PRIVATE_KEY` / `GOOGLE_SHEET_ID` 도 예시값
  - 채워진 것은 `GMAIL_USER` / `RECIPIENT_EMAIL`(둘 다 woogs4444@gmail.com)뿐
- ⚠️ **참고: `.env`에 `PORT=3000`이 있고 server.js가 `dotenv.config({override:true})`를 써서, 터미널에서 `PORT=3001`로 지정해도 무시되고 3000으로 뜹니다.** 포트를 바꾸려면 `.env`를 고쳐야 함.
- **미리보기(8000, 파이썬)와 신청서버(3000, node)의 연동은 이미 되어 있음** — `app.js:3`에서 8000번으로 열면 자동으로 `http://127.0.0.1:3000`으로 신청을 보냄. 실제 POST 테스트로 확인 완료.

### 🐞 발견하고 고친 버그 (2026-08-04)
- 증상: 신청을 넣으면 **성공이 아니라 실패(500)** 가 떴음. 진짜 신청자도 빨간 실패 화면을 봤을 상태였음.
- 원인 2가지
  1. 예시값 판별 함수(`hasConfiguredValue`)가 `PUT_YOUR_..._HERE` 같은 **밑줄형 예시 문구를 못 걸러냄**
     → 서버가 "비밀번호 채워졌다"고 착각하고 가짜 값으로 지메일 로그인 시도 → 구글이 `535 BadCredentials` 거부
     (패턴 목록에 `'YOUR_'`가 있었지만 비교 직전에 소문자로 바꿔서 영영 매칭되지 않는 죽은 패턴이었음)
  2. 메일 발송에 try/catch가 없어서, **메일 하나 실패하면 신청 접수 전체가 실패** 처리됨
- 조치: 예시값 패턴을 소문자·밑줄형까지 보강 + 메일 발송을 각각 try/catch로 감쌈
- 결과: 지금은 메일 설정이 없어도 **신청은 정상 접수되고 로컬에 저장**됨 (`.data/applications.json`, 테스트 포함 4건)

## 3-1. ✅ 접수 방식 변경 확정 (2026-08-04) — node 서버 → **구글 Apps Script**
- 이유: 기존 구조는 **내 PC에서 node 서버가 켜져 있어야만** 신청이 접수됨 → PC 끄면 폼이 먹통.
- 옆 프로젝트 `..\LandingPage`(감귤박)에서 검증된 Apps Script 방식을 재활용
  (서버 없음·비용 없음·24시간 동작, 시트 저장＋알림메일＋자동회신 3종 동시 처리).
- 이 전환으로 `.env`·node 서버·구글 서비스계정·private key가 **전부 불필요**해짐.

### 🔑 계정 주의 (헷갈리기 쉬움)
| | 감귤박 페이지 | **제주다회 (이 폴더)** |
|---|---|---|
| 접수 계정 | `suchang.marketing@gmail.com` (회사) | **`woogs4444@gmail.com` (개인)** |

- 서로 **다른 스크립트·다른 시트**다. 반드시 개인 계정으로 로그인한 상태에서 만들 것.
- 예전에 감귤박 작업 중 **개인 계정에 잘못 만든 스크립트·시트가 방치**돼 있음 → 이번에 보이면 삭제할 것.

### 만들어 둔 것 (2026-08-04)
- `apps-script\Code.gs` — 제주다회용 접수 코드 (폼 4칸: 이름·이메일·연락처·메모)
- `📮 참가신청폼 설치안내.md` — 10분짜리 화면 단계별 설치 문서
- `app.js` — 로컬 3000 서버 대신 **웹앱 주소로 전송**하도록 교체. 9번째 줄 `GAS_URL`이 아직 빈 값
- `index.html` + `styles.css` — 봇 차단 칸(허니팟) + **개인정보 수집·이용 동의 체크박스** 추가

### 이제 쓰지 않는 것 (지워도 되지만 일단 남겨둠)
- `server.js`, `package.json`, `node_modules`, `.env`, `SETUP_GUIDE.md`, `.data\applications.json`
- Apps Script 설치가 끝나 정상 작동을 확인한 뒤에 정리하는 게 안전함

## 4. 주요 파일
- index.html: 랜딩페이지 구조
- styles.css: 디자인 스타일
- app.js: 폼 제출 처리
- server.js: 신청 API 및 자동화 로직
- .env: Gmail / Google 인증값 저장소
- SETUP_GUIDE.md: Gmail, Google Sheets 설정 가이드

## 5. 현재 확인된 이슈
- Gmail 앱 비밀번호는 .env에 등록되어 있지만, 실제 메일 전송 테스트가 완료되었는지는 추가 확인이 필요합니다.
- Google Sheets 연동은 서비스 계정 이메일, private key, sheet ID가 실제 값으로 들어와야 합니다.
- 서버 재시작 후 .env 값이 반영되는지 확인이 필요합니다.

## 6. 다음에 할 일
1. 🔴 **우경님이 직접 하실 것 — `📮 참가신청폼 설치안내.md` 따라 Apps Script 설치 (10분)**
   → **개인 계정(woogs4444)** 으로 로그인 확인 → `sheets.new`로 시트 생성 → 확장 프로그램 → Apps Script
   → `apps-script\Code.gs` 붙여넣기 → `테스트_신청보내기` 실행해서 시트·메일 확인
   → 배포 → 새 배포 → 웹 앱 → **액세스 권한 "모든 사용자"** → **웹앱 주소 복사**
2. 받은 웹앱 주소를 `app.js` 9번째 줄 `GAS_URL`에 넣기 (클로드에게 주소를 주면 대신 넣어줌)
3. 실제 폼으로 신청 1건 테스트 (`Ctrl+Shift+R` 강력 새로고침 후)
4. 배포 위치 결정 — 이제 서버가 필요 없으므로 감귤박처럼 **Vercel 정적 배포 가능**
   (단 감귤박이 저장소 루트를 `vercel.json`으로 `LandingPage`에 물려둔 상태라, 같이 올리려면 경로 정리가 필요함)
5. 정상 작동 확인 후 `server.js`·`node_modules`·`.env` 등 안 쓰는 파일 정리

## 7. 실행 방법
- 서버 실행:
  - node server.js
- 정적 미리보기:
  - python -m http.server 8000

## 8. 참고사항
- 신청 데이터는 로컬 백업 파일(.data/applications.json)에 저장됩니다.
- 메일/시트 자동화는 .env 값이 정상일 때만 동작합니다.
- 문제가 생기면 먼저 .env 값과 server.js의 설정 상태를 확인하세요.
