# 🔧 날씨앱 진행상황

## 기본정보

- 위치: `D:\CLAUDE\jeju_vibe\weatherApp`
- 무엇: 여러 도시를 즐겨찾기 해두고 오늘·24시간·7일 날씨와 미세먼지를 한 화면에서 보는 반응형 웹앱
- 기획서: [PRD.md](PRD.md)
- 데이터: **Open-Meteo** — API 키·회원가입 불필요, 비상업 무료 (하루 1만 건)
  - 예보 `api.open-meteo.com/v1/forecast`
  - 미세먼지 `air-quality-api.open-meteo.com/v1/air-quality`
  - 도시검색 `geocoding-api.open-meteo.com/v1/search`
- 기술: Next.js 16(App Router) + TypeScript + Tailwind v4 + **shadcn/ui** + **lucide-react**
- 로컬 실행: 폴더에서 `npm run dev` → 브라우저에서 http://localhost:3000

## 디자인 방향 (2026-08-05 우경님 확정)

- 디자인 시스템: **shadcn/ui**, 기본 색상 **Slate**, 아이콘 **Lucide**
- 배경: **날씨·낮밤에 따라 바뀌는 그라데이션** 8종(맑음낮/맑음밤/흐림낮/흐림밤/비/눈/안개/뇌우), 1초 크로스페이드
- 밝기 모드: **낮/밤 자동** — 그 도시가 밤이면 자동으로 다크모드
- 카드: 배경이 은은히 비치는 유리 느낌(`.glass`), 카드들은 **좌우로 배치**
- 폰트: Pretendard(한글) + Geist

## 완료한 것 (2026-08-05)

- [x] PRD 작성 (육하원칙 + 1페이지 5항목)
- [x] Next.js + Tailwind + shadcn/ui + lucide-react 셋업 (Slate 테마 적용)
- [x] `lib/weather.ts` — Open-Meteo 연결, WMO 날씨코드 한글화, 온도 변환, 미세먼지 등급
- [x] 화면 전체 구현
  - 도시 검색(300ms 자동검색) + 즐겨찾기 칩(추가·삭제·전환, localStorage 저장)
  - 현재 날씨 큰 카드 / 오늘 상세 6칸(체감·강수확률·습도·바람·자외선·일출일몰)
  - 24시간 예보 가로 스크롤 24칸
  - 7일 예보 7칸 + 기온 범위 막대 / 미세먼지 카드(PM2.5·PM10, 환경부 4단계)
  - °C/°F 전환, 새로고침, 로딩 스켈레톤, 오류 안내
- [x] `npm run build` 통과 + 로컬 서버 200 응답 + Open-Meteo 실제 응답 확인
- [x] **인터랙션 추가** (우경님 요청 "좀 인터랙션이 있었으면")
  - 24시간 **기온 꺾은선 그래프** + 마우스/터치 위치의 시각·기온·강수확률 툴팁, 최고·최저 지점 표시
  - **날씨별 배경 효과**: 비·눈 내림, 밤 별 반짝임, 맑은 낮 햇살, 흐림 구름 흐름, 뇌우 번쩍임
  - 기온 숫자 **카운트업**, 큰 아이콘 은은한 움직임(해 회전·비 흔들림)
  - **7일 카드 클릭 → 그날 상세 펼침** (강수확률·자외선·일출·일몰)
  - 시간별 카드 드래그 스크롤 + 좌우 화살표, 카드·칩 호버/누름 반응, 도시 전환 시 재등장 애니메이션
  - 접근성: 시스템에서 "동작 줄이기"를 켠 사람에겐 애니메이션 자동 꺼짐

- [x] **깃허브 push + Vercel 배포 완료 (2026-08-05)**
  - 공개 주소: **https://weather-app-sigma-three-65.vercel.app**
  - Vercel 프로젝트명: `weather-app` (팀 `woowooff1`)
  - 깃허브: 기존 저장소 `woowooff/landingpage2`의 `weatherApp/` 폴더에 함께 있음

## 배포하는 법 (다음에도 이렇게)

```bash
cd D:\CLAUDE\jeju_vibe\weatherApp
vercel deploy --prod
```

> ⚠️ 이 프로젝트는 **깃허브 자동배포에 연결되어 있지 않다.** (저장소 루트가 `jeju_vibe`인데 앱은 하위 폴더라, 자동배포를 쓰려면 Vercel 대시보드에서 프로젝트 → Settings → Git → 저장소 연결 후 **Root Directory를 `weatherApp`으로** 지정해야 함.)
> 지금은 위 명령 한 줄로 배포하면 되고, 코드는 `git push`로 따로 백업된다.
> `weatherApp/.vercel`, `.env.local`은 깃에 올라가지 않도록 무시 처리되어 있음(확인 완료).

## 챗봇 (날씨 비서) — 2026-08-05 추가

- 무엇: 오른쪽 아래 말풍선 버튼 → 지금 보고 있는 도시의 **실제 예보 데이터를 근거로** 답하는 날씨 비서
- 모델: **Gemini 3.5 Flash-Lite** (가장 저렴·빠름), **무료 등급만 사용 — 구글에 결제수단 등록하지 않음** (우경님 결정)
- 구조: 브라우저 → `/api/chat`(우리 서버) → 제미나이. **API 키는 서버에만 있고 브라우저로 안 내려감**
- 안전장치: 한 사람 하루 30회 / 전체 하루 300회, 질문 500자 제한, 한도 초과 시 요금이 아니라 안내 메시지
- 파일: `app/api/chat/route.ts`(서버), `components/weather/weather-chat.tsx`(채팅창), `lib/weather.ts`의 `buildWeatherContext()`(근거 요약)
- 🔴 **키 넣기 안내: [🔑 챗봇 API 키 설치안내.md](🔑%20챗봇%20API%20키%20설치안내.md) — 우경님이 직접 하실 것**

## 다음에 할 것

- [ ] 🔴 **우경님: 제미나이 무료 API 키 발급 → `.env.local`에 붙여넣기 → 서버 재시작** (위 설치안내 ①②)
- [ ] 🔴 **우경님: Vercel 환경변수에 같은 키 넣고 재배포** (설치안내 ③) — 안 하면 배포된 사이트에선 챗봇이 "키가 없어요"라고 답함
- [ ] **우경님이 배포된 주소를 폰·PC에서 확인** — https://weather-app-sigma-three-65.vercel.app
- [ ] 고칠 점 알려주기 (색·글자 크기·카드 순서·인터랙션 강도 등)
- [ ] (선택) 깃허브 자동배포 연결 — 위 "배포하는 법" 경고 박스대로 Root Directory 지정
- [ ] (선택) 주소를 짧게: Vercel 프로젝트 Settings → Domains에서 이름 바꾸기
- [ ] (선택) "현재 위치로 보기" 버튼

## 특이사항

- 폴더명 `weatherApp`에 **대문자**가 있어 `create-next-app`이 거부함 → 임시 폴더에 만든 뒤 옮겨서 해결. `package.json`의 이름은 `weather-app`.
- 미세먼지는 지역에 따라 값이 없을 수 있음 → 없으면 "정보를 제공하지 않아요"로 표시(날씨는 정상 표시).
- API 키가 없으므로 `.env` 파일 자체가 필요 없음 = 유출 위험 없음.
